[project]
name = "hobbit-ng"
version = "3.1.0"
description = "Authorized security assessment and posture-analysis framework"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
